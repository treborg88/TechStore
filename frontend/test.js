const http = require('http'); http.get('http://localhost:5001/api/products', (res) = let data = ''; res.on('data', chunk = += chunk); res.on('end', () = console.log('Response length:', data.length); try { const products = JSON.parse(data); console.log('Products:', products.length); console.log('First product images:', products[0].images); } catch(e) { console.log('Error:', e.message); } }); });  
